# Deploy is handled automatically by Vercel's native Git integration.
# Vercel watches the main branch and deploys on every push — no action needed here.
# This file is kept as a placeholder so it's clear the workflow was intentionally removed.
name: Deploy to Vercel
on:
  workflow_dispatch:  # manual only — auto-deploy is via Vercel Git integration
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Vercel deploys automatically on push to main via Git integration."
