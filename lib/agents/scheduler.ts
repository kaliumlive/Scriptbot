// Scheduling is manual — the app is analytics read-only.
// Stub kept so existing references compile without changes.
export async function runScheduler(_brandId?: string) {
  return { scheduledCount: 0 }
}
