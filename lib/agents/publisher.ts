// Publishing is manual — the app is analytics read-only.
// Stub kept so existing references compile without changes.
export async function runPublisher(_brandId?: string) {
  return { publishedCount: 0 }
}
